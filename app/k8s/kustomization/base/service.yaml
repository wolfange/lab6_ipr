apiVersion: v1
kind: Service
metadata:
  name: sales-aggregator
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sales-aggregator
